---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: gcpkccmanagedcontrolplanetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: GCPKCCManagedControlPlaneTemplate
    listKind: GCPKCCManagedControlPlaneTemplateList
    plural: gcpkccmanagedcontrolplanetemplates
    shortNames:
    - gcpkccmcpt
    singular: gcpkccmanagedcontrolplanetemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: GCPKCCManagedControlPlaneTemplate is the Schema for the GCPKCCManagedControlPlaneTemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GCPKCCManagedControlPlaneTemplateSpec defines the desired
              state of GCPKCCManagedControlPlaneTemplate.
            properties:
              template:
                description: GCPKCCManagedControlPlaneTemplateResource describes the
                  data needed to create an GCPKCCManagedControlPlane from a template.
                properties:
                  spec:
                    description: GCPKCCManagedControlPlaneTemplateResourceSpec defines
                      the desired state of GCPKCCManagedControlPlane for use in a
                      template.
                    properties:
                      containerCluster:
                        description: ContainerCluster defines the KCC ContainerCluster
                          resource.
                        properties:
                          metadata:
                            description: Metadata for the KCC ContainerCluster resource.
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations is an optional set of annotations
                                  on the KCC resource.
                                type: object
                              labels:
                                additionalProperties:
                                  type: string
                                description: Labels is an optional set of labels on
                                  the KCC resource.
                                type: object
                              name:
                                description: Name is the name of the KCC resource.
                                type: string
                              namespace:
                                description: Namespace is the namespace of the KCC
                                  resource.
                                type: string
                            type: object
                          spec:
                            description: Spec defines the desired state of the KCC
                              ContainerCluster.
                            properties:
                              additionalConfig:
                                description: AdditionalConfig allows passing additional
                                  KCC fields not covered by the typed schema.
                                x-kubernetes-preserve-unknown-fields: true
                              initialNodeCount:
                                description: InitialNodeCount is the number of nodes
                                  to create in this cluster's default node pool.
                                format: int32
                                type: integer
                              ipAllocationPolicy:
                                description: IpAllocationPolicy configures IP allocation
                                  for pods and services.
                                properties:
                                  clusterSecondaryRangeName:
                                    description: ClusterSecondaryRangeName is the
                                      name of the secondary range for pod IPs.
                                    type: string
                                  servicesSecondaryRangeName:
                                    description: ServicesSecondaryRangeName is the
                                      name of the secondary range for service IPs.
                                    type: string
                                type: object
                              location:
                                description: Location is the region or zone for the
                                  GKE cluster.
                                type: string
                              loggingService:
                                description: LoggingService is the logging service
                                  the cluster should use.
                                type: string
                              masterAuthorizedNetworksConfig:
                                description: MasterAuthorizedNetworksConfig configures
                                  master authorized networks.
                                properties:
                                  cidrBlocks:
                                    description: CidrBlocks defines the list of CIDR
                                      blocks allowed to access the master.
                                    items:
                                      description: KCCMasterAuthorizedNetworksCidrBlock
                                        defines a CIDR block for master authorized
                                        networks.
                                      properties:
                                        cidrBlock:
                                          description: CidrBlock is an external network
                                            that can access the master.
                                          type: string
                                        displayName:
                                          description: DisplayName is a display name
                                            for the CIDR block.
                                          type: string
                                      type: object
                                    type: array
                                type: object
                              minMasterVersion:
                                description: MinMasterVersion is the minimum version
                                  of the master. Managed by CAPI version override.
                                type: string
                              monitoringService:
                                description: MonitoringService is the monitoring service
                                  the cluster should use.
                                type: string
                              networkRef:
                                description: NetworkRef is a reference to the VPC
                                  network for this cluster.
                                properties:
                                  name:
                                    description: Name is the name of the referenced
                                      KCC resource.
                                    type: string
                                type: object
                              networkingMode:
                                description: 'NetworkingMode configures the networking
                                  mode. Possible values: VPC_NATIVE, ROUTES.'
                                type: string
                              privateClusterConfig:
                                description: PrivateClusterConfig configures private
                                  cluster settings.
                                properties:
                                  enablePrivateEndpoint:
                                    description: EnablePrivateEndpoint controls whether
                                      the master's internal IP is used as the cluster
                                      endpoint.
                                    type: boolean
                                  enablePrivateNodes:
                                    description: EnablePrivateNodes controls whether
                                      nodes have internal IP addresses only.
                                    type: boolean
                                  masterIpv4CidrBlock:
                                    description: MasterIpv4CidrBlock is the IP range
                                      in CIDR notation for the master network.
                                    type: string
                                type: object
                              releaseChannel:
                                description: ReleaseChannel configures the release
                                  channel for automatic upgrades.
                                properties:
                                  channel:
                                    description: 'Channel is the release channel.
                                      Possible values: RAPID, REGULAR, STABLE, EXTENDED.'
                                    type: string
                                type: object
                              removeDefaultNodePool:
                                description: RemoveDefaultNodePool controls whether
                                  to delete the default node pool after cluster creation.
                                type: boolean
                              subnetworkRef:
                                description: SubnetworkRef is a reference to the VPC
                                  subnetwork for this cluster.
                                properties:
                                  name:
                                    description: Name is the name of the referenced
                                      KCC resource.
                                    type: string
                                type: object
                              workloadIdentityConfig:
                                description: WorkloadIdentityConfig configures workload
                                  identity for the cluster.
                                properties:
                                  workloadPool:
                                    description: WorkloadPool is the workload identity
                                      pool.
                                    type: string
                                type: object
                            type: object
                        type: object
                      version:
                        description: Version is the Kubernetes version for the control
                          plane.
                        type: string
                    required:
                    - containerCluster
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
