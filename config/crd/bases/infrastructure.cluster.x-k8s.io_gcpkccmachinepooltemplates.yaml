---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  labels:
    cluster.x-k8s.io/v1beta2: v1beta2
  name: gcpkccmachinepooltemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: GCPKCCMachinePoolTemplate
    listKind: GCPKCCMachinePoolTemplateList
    plural: gcpkccmachinepooltemplates
    shortNames:
    - gcpkccmpt
    singular: gcpkccmachinepooltemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: GCPKCCMachinePoolTemplate is the Schema for the gcpkccmachinepooltemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GCPKCCMachinePoolTemplateSpec defines the desired state of
              GCPKCCMachinePoolTemplate.
            properties:
              template:
                description: GCPKCCMachinePoolTemplateResource describes the data
                  needed to create a GCPKCCMachinePool from a template.
                properties:
                  spec:
                    description: |-
                      GCPKCCMachinePoolSpec defines the desired state of GCPKCCMachinePool.

                      Users provide a complete Config Connector ContainerNodePool resource specification.
                      CAPG creates the resource, waits for it to be ready, then populates ProviderIDList
                      by listing Node objects in the workload cluster.

                      The Config Connector resource must include the "cnrm.cloud.google.com/project-id"
                      annotation to indicate which GCP project to use.
                    properties:
                      nodePool:
                        description: |-
                          NodePool is a complete Config Connector ContainerNodePool resource spec.
                          CAPG creates this resource and manages its lifecycle via owner references.

                          Example:
                            apiVersion: container.cnrm.cloud.google.com/v1beta1
                            kind: ContainerNodePool
                            metadata:
                              name: my-nodepool
                              annotations:
                                cnrm.cloud.google.com/project-id: "my-gcp-project"
                            spec:
                              location: us-central1
                              clusterRef:
                                name: my-cluster
                              initialNodeCount: 3
                        type: object
                        x-kubernetes-embedded-resource: true
                        x-kubernetes-preserve-unknown-fields: true
                      providerIDList:
                        description: |-
                          ProviderIDList contains the provider IDs for each node in this node pool.
                          Format: gce://<project>/<zone>/<instance-name>
                          Populated by the controller after the node pool is ready and workload cluster
                          nodes are available. This is a MANDATORY field per the CAPI v1beta2 InfraMachinePool contract.
                        items:
                          type: string
                        type: array
                    required:
                    - nodePool
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
